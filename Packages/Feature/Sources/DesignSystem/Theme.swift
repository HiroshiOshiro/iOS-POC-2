import SwiftUI

/// NiA 相当: core:designsystem の theme（`Color` / `NiaTheme`）。
public extension Color {
    /// アプリのブランドカラー（ObjC 側 AppAppearance と同値）。
    static let brand = Color(red: 0.31, green: 0.27, blue: 0.90)
}
